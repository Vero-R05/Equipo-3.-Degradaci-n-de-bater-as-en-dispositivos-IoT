# Código enfocado en el enfoque numérico con el método de Euler

# Se utilizaron 3 librerías que hay que exportarse en la terminal para que el código funcione con el comando al principio "pip install NombreLibreria" 
# las cuales fueron las siguientes:
# "numpy"
# "pandas"
# "matplotlib"
